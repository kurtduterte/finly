// Bundled model asset — downloaded once by the developer,
// never fetched at runtime
const kGemmaAssetPath = 'assets/models/gemma3-1b-it-int4.task';

// 1024 tokens fits a receipt-length prompt + short JSON response comfortably
const kGemmaMaxTokens = 1024;
